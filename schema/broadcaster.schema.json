{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "broadcaster.schema.json",
  "title": "Broadcaster",
  "description": "转播方信息",
  "type": "object",
  "required": ["id", "name", "platform", "url", "note"],
  "properties": {
    "id":       { "type": "string" },
    "name":     { "type": "string" },
    "platform": { "type": "string" },
    "url":      { "type": "string" },
    "note":     { "type": "string" }
  },
  "additionalProperties": true
}
